\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}

\usepackage{amssymb, amsthm}
\usepackage[table]{xcolor} % Colorear solo una celda de una tabla
\usepackage{geometry} % Márgenes en las páginas
\usepackage[backend=biber]{biblatex} % Poder agregar la bibliografia con un .bib
\usepackage{graphicx} % Para poder agregar las imágenes con y usar \graphicspath
\usepackage{lipsum} % Usar texto dummy para llenar hojas
\usepackage{minted} % Para agregar código
\usemintedstyle{perldoc} % Tema de minted
\usepackage[ruled, vlined, linesnumbered]{algorithm2e} % Incluir pseudocódigo
\usepackage{newunicodechar} % Poder usar (pero debemos declarar también) caracteres unicode en código
\usepackage{pdfpages} % Poder insertar pdfs.
\usepackage{tcolorbox} % Poder poner contenido en una caja
\usepackage{color} % Referirnos a colores con un alias
\usepackage{hyperref} % Hace hipervínculos.
\hypersetup{
    linktoc=all,
    linkcolor=blue,
}
\usepackage{epigraph} % Poner epígrafe en los capítulos
\usepackage[toc,page,header]{appendix} % Poder agregar el apéndice como un capítulo
\addto\captionsspanish{ % El paquete de appendix está en inglés y se debe sobreescribe el nombre en español
  \renewcommand\appendixname{Apéndices}
  \renewcommand{\appendixtocname}{Apéndices}
  \renewcommand\appendixpagename{Apéndices}
}

\geometry{bindingoffset=3cm}

\addbibresource{referencias.bib}
\graphicspath{{imagenes/}}

% Usando caracteres unicode, en el pdf le pone unn rectángulo como si fuera una referencia,
% entonces con este hack se quita eso.
% Fuente: https://tex.stackexchange.com/questions/343494/minted-red-box-around-greek-characters
\usepackage{etoolbox,xpatch}
\makeatletter
\AtBeginEnvironment{minted}{\dontdofcolorbox}
\def\dontdofcolorbox{\renewcommand\fcolorbox[4][]{##4}}
\xpatchcmd{\inputminted}{\minted@fvset}{\minted@fvset\dontdofcolorbox}{}{}
\xpatchcmd{\mintinline}{\minted@fvset}{\minted@fvset\dontdofcolorbox}{}{} % see https://tex.stackexchange.com/a/401250/
\makeatother
%

% Símbolos unicode que usaré en código que está en \minted
\newunicodechar{⊑}{\ensuremath{\sqsubseteq}}
\newunicodechar{↓}{\ensuremath{\downarrow}}
\newunicodechar{∈}{\ensuremath{\in}}

% Macro para código inline de Haskell
\newcommand{\hsCode}[1]{\mintinline{haskell}|#1|}

% Macros para usar otra tipografía en algunas palabras
\newcommand{\QuickCheck}{{\fontfamily{lmss}\selectfont QuickCheck }}